1. Разница SQL MySQL

SQL - язык стуктурированых запросов, используемый для работы, обработки и ввода баз данных. 
      язык для управления системой администрирования реляционных баз данных.
      язык структурированных запросов, с помощью него пишутся специальные запросы (SQL инструкции) к базе данных с целью получения этих данных из базы и для манипулирования этими данными.

MySQL - система администрирования реляционной базы данных с открытым исходным кодом, которая использует инструкции SQL.

2. Разница Delete Truncate

TRUNCATE — в языке SQL — операция мгновенного удаления всех строк в таблице. 
Общий синтаксис команды:

TRUNCATE TABLE <Имя Таблицы>

DELETE — операция удаления записей из таблицы. Критерий отбора записей для удаления определяется выражением where. В случае, если критерий отбора не определён, выполняется удаление всех записей.

Общий синтаксис команды:

DELETE FROM <Имя Таблицы> WHERE <Условие отбора записей>

3. DDL DML DCL

Data Definition Language (DDL) - это группа команд определения данных. Другими словами, с помощью команд, входящих в эту группы, мы определяем структуру базы данных и работаем с объектами этой базы, т.е. создаем, изменяем и удаляем их.

В эту группу входят следующие команды:

CREATE - используется для создания объектов базы данных;
ALTER - используется для изменения объектов базы данных;
DROP - используется для удаления объектов базы данных.

Data Manipulation Language (DML) - это группа команд для манипуляции данными. С помощью этих команд мы можем добавлять, изменять, удалять и выгружать данные из базы, т.е. манипулировать ими.

В эту группу входят самые распространённые команды языка SQL:

SELECT - осуществляет выборку данных;
INSERT - добавляет новые данные;
UPDATE - изменяет существующие данные;
DELETE - удаляет данные.

Data Control Language (DCL) - группа команд определения доступа к данным. Иными словами, это команды для управления разрешениями, с помощью них мы можем разрешать или запрещать выполнение определенных операций над объектами базы данных.

Сюда входят:

GRANT - предоставляет пользователю или группе разрешения на определённые операции с объектом;
REVOKE - отзывает выданные разрешения;
DENY - задаёт запрет, имеющий приоритет над разрешением.

Transaction Control Language (TCL) - группа команд для управления транзакциями. Транзакция - это команда или блок команд (инструкций), которые успешно завершаются как единое целое, при этом в базе данных все внесенные изменения фиксируются на постоянной основе или отменяются, т.е. все изменения, внесенные любой командой, входящей в транзакцию, будут отменены.

Группа команд TCL предназначена как раз для реализации и управления транзакциями. Сюда можно отнести:

BEGIN TRANSACTION - служит для определения начала транзакции;
COMMIT TRANSACTION - применяет транзакцию;
ROLLBACK TRANSACTION - откатывает все изменения, сделанные в контексте текущей транзакции;
SAVE TRANSACTION - устанавливает промежуточную точку сохранения внутри транзакции.

4. два типа субд рассказать

5. Что такое таблица и поле в табице

Таблица - объект, который содержит все данные в базах данных. В таблицах данные логически организованы в виде строк и столбцов по аналогии с электронной таблицей. Каждая строка представляет собой уникальную запись, а каждый столбец — поле записи.

Число таблиц в базе данных ограничено только числом объектов доступных в базе данных (2 147 483 647). Стандартная определяемая пользователем таблица может содержать до 1 024 столбцов. Число строк и общий размер таблицы ограничиваются только пространством для хранения на сервере.

Можно также устанавливать свойства для таблицы и каждого столбца в таблице для управления допустимыми данными и другими свойствами.

Свойства полей базы данных:

Имя поля (Длина имени не может превышать 64 символа. В именах полей можно использовать любые комбинации букв, цифр, пробелов и других символов, за исключением символов «.», «!», «”», «[» и «]».);
Тип поля (определяет тип данных, которые могут содержаться в данном поле.);
Типы данных (Текстовый, длиной от 1 до 255 символов, по умолчанию 50. Числовой. Дата/время. Денежный. Счетчик, пользователь не может сам модифицировать значения поля данного типа. Логический. Поле МЕМО (текстовое поле произвольной длины, но не более 256 символов);
Размер поля (предельная длина (в символах) данных);
Маска ввода (это шаблон, позволяющий вводить в поле значения, имеющие одинаковый формат);
Значение по умолчанию;
Условие на значение;
Сообщение об ошибке;
Обязательное поле;
Пустые строки;
Индексированное поле (для индексированных полей можно сделать так, что значения в записях будут проверяться по этому полю на наличие повторов, что позволяет автоматически исключить дублирование данных)


6. Разница Char VarChar

Параметры 
сравнения		Чар			Варчар

Смысл			Это код SQL, 		Это код SQL,
			который помогает 	который помогает
			хранить символы		хранить переменные символы.		

Аббревиатура 
для			символ			переменный символ

Размер хранилища	Они хранят значения 	Они хранят буквенно-цифровые 
			фиксированной длины 	данные переменной длины и
			такие как номера 	зависят от конкретной строки,
			телефонов		которая хранится.
			и равны максимуму 	такие как адрес
			столбца. 	  	

Выделение памяти	Распределение 		Динамическое 
			статической памяти	распределение памяти			

Используемые байты	1 байт на символ	1 байт на символ плюс 1 или 2
						дополнительных байта для 
						хранения информации о
						длине.

Лимит символов		255			65535

Применение		Программисты могут 	Программисты могут
			использовать это, 	использовать это,
			когда длина символов  	когда длина записей данных
			кратка и известна.	варьируется.

Содержание		Имеет только 		Имеет различные строки, такие
			определенные строковые	как алфавиты, числа и переменные
			символы	
			
			лучшая 
			производительность

7. Primary Key and Forein Key Разница Свойства

целостность БД - это свойство БД, которое обеспечивает корректность и непротиворечивость данных в любой момент времени.

Существует 3 вида целостности баз данных: доменная целостность, целостность сущностей и ссылочная целостность. 

Доменная целостность: CHECK(ограничение проверки), DEFAULT(запись значения по-умолчанию).
Целостность сущностей: Primary Key(первичный ключ), UNIQUE(уникальность).
Ссылочная целостность: Foreign Key(внешний ключ).

Primary Key - (он же - первичный ключ) - предоставляет ссылку для связи с другими таблицами и создаёт ограничение уникальности для столбца, на котором задаётся.

8. Что такое Constrante, такое ограничения при создании таблицы какие они бывают?

9. Три формы нормализации данных

10. Drop Trancate Разница

11. Какие операторі доступны в SQL три

12. Что такое Null

13. Что такое подзапросы?